
<nav class="bg-white shadow-md px-6 py-4 sticky top-0 z-50">
  <div class="max-w-7xl mx-auto flex items-center justify-between flex-wrap">

    <!-- Left: Logo -->
    <div class="flex items-center gap-2">
      <a href="/listings">
        <img src="https://upload.wikimedia.org/wikipedia/commons/6/69/Airbnb_Logo_B%C3%A9lo.svg" 
             alt="Airbnb Logo" 
             class="h-8 w-auto" />
      </a>
    </div>

   <form action="/listings/search" method="GET" class="w-full md:w-auto mt-4 md:mt-0 flex justify-center md:justify-center order-3 md:order-2 novalidate">
  <div class="flex border border-gray-300 rounded-full overflow-hidden  w-full max-w-md">
    <input 
      type="text" 
      name="q" 
      placeholder="Search by country ..." 
      class="px-4 py-2 w-full focus:outline-none text-gray-700"
      required
    />
    <button 
      type="submit"
      class="flex items-center gap-2 px-4 py-2 text-white font-semibold transition"
      style="background-color: #FF5A5F;" 
    >
      <svg 
        class="w-5 h-5" 
        fill="none" 
        stroke="currentColor" 
        stroke-width="2" 
        viewBox="0 0 24 24"
      >
        <path 
          stroke-linecap="round" 
          stroke-linejoin="round" 
          d="M21 21l-4.35-4.35M17 10a7 7 0 11-14 0 7 7 0 0114 0z" 
        />
      </svg>
      Search
    </button>
  </div>
</form>

    <!-- Right: Links -->
    <div class="hidden md:flex items-center gap-6 order-3 md:order-3">
      <a href="/listings/new" class="px-4 py-2 rounded-full border border-gray-300 hover:border-pink-600 hover:text-pink-600 transition-all duration-300">
        Airbnb your home
      </a>

      <% if (!currUser) { %>
        <a href="/signup" class="px-4 py-2 rounded-full border border-blue-500 text-blue-500 hover:bg-blue-500 hover:text-white transition duration-300">
          Sign Up
        </a>
        <a href="/login" class="px-4 py-2 rounded-full border border-blue-500 text-blue-500 hover:bg-blue-500 hover:text-white transition duration-300">
          Login
        </a>
      <% } %>

      <% if (currUser) { %>
        <a href="/logout" class="px-4 py-2 rounded-full border border-pink-500 text-pink-500 hover:bg-pink-500 hover:text-white transition duration-300">
          Logout
        </a>
      <% } %>
    </div>

    <!-- Mobile Menu Button -->
    <div class="block md:hidden order-2 md:order-4 ml-auto">
      <button id="menu-btn" class="text-gray-700 focus:outline-none text-2xl">
        ☰
      </button>
    </div>

    <!-- Mobile Menu Links -->
    <div id="menu" class="w-full mt-4 hidden md:hidden">
      <div class="flex flex-col gap-4 text-gray-700 text-base font-medium">
        <a href="/listings/new" class="px-4 py-2 rounded-full border border-gray-300 hover:border-pink-600 hover:text-pink-600 transition-all duration-300">
          Airbnb your home
        </a>
        <% if (!currUser) { %>
          <a href="/signup" class="px-4 py-2 rounded-full border border-blue-500 text-blue-500 hover:bg-blue-500 hover:text-white transition duration-300">
            Sign Up
          </a>
          <a href="/login" class="px-4 py-2 rounded-full border border-blue-500 text-blue-500 hover:bg-blue-500 hover:text-white transition duration-300">
            Login
          </a>
        <% } %>
        <% if (currUser) { %>
          <a href="/logout" class="px-4 py-2 rounded-full border border-pink-500 text-pink-500 hover:bg-pink-500 hover:text-white transition duration-300">
            Logout
          </a>
        <% } %>
      </div>
    </div>

  </div>
</nav>

<script>
  // Toggle mobile menu
  document.addEventListener("DOMContentLoaded", function () {
    const btn = document.getElementById("menu-btn");
    const menu = document.getElementById("menu");

    btn.addEventListener("click", () => {
      menu.classList.toggle("hidden");
    });
  });
</script>
